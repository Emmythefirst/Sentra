# Zerion API key — https://dashboard.zerion.io
ZERION_API_KEY=zk_dev_...

# Solana RPC (defaults to public mainnet-beta if not set — use a private RPC for production)
SOLANA_RPC=https://api.mainnet-beta.solana.com

# Express server port
PORT=3001
